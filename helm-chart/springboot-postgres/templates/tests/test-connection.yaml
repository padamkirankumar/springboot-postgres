apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springboot-postgres.fullname" . }}-test-connection"
  labels:
    {{- include "springboot-postgres.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springboot-postgres.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
